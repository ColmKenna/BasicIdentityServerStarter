@page "{id}"
@model IdentityServerAspNetIdentity.Pages.Admin.Accounts.Details

<tab id="user-details">
    <tab-item header="Account Information" tab-id="account-information">
        <field-container label="Username" for="@Model.ApplicationUserDetails.UserName" />
        <field-container label="Email" for="@Model.ApplicationUserDetails.Email" />
        <field-container label="Phone Number" for="@(Model.ApplicationUserDetails.PhoneNumberConfirmed ? Model.ApplicationUserDetails.PhoneNumber : "Not confirmed")" />
        <field-container label="Two Factor Enabled" for="@Model.ApplicationUserDetails.TwoFactorEnabled" />
        <field-container label="Lockout Enabled" for="@Model.ApplicationUserDetails.LockoutEnabled" />
        <field-container label="Lockout Ends" value="@(Model.ApplicationUserDetails.LockoutEnd?.ToString("g") ?? "Not applicable")" />
        <field-container label="Access Failed Count" for="@Model.ApplicationUserDetails.AccessFailedCount" />
    </tab-item>

    <tab-item header="Claims" tab-id="claims">
        @foreach (var claim in Model.ApplicationUserDetails.UserClaims)
        {
            <field-container label="@claim.ClaimType" for="@claim.ClaimValue" />
        }
    </tab-item>


    <tab-item header="Roles" tab-id="roles">
        @foreach (var role in Model.ApplicationUserDetails.Roles)
        {
            <field-container label="Role" for="@role" />
        }
    </tab-item>
</tab>